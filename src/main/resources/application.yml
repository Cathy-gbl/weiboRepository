###服务启动端口号
server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/weibo?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: etoak
    driver-class-name: com.mysql.jdbc.Driver

  mvc:
    view:
      prefix=classpath:/templates/:
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: HTML5
    servlet:
      content-type: text/html